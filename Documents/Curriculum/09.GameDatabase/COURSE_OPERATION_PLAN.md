# 게임 데이터베이스 프로그래밍 운영 계획

이 문서는 `26국기_교수계획서.pdf`의 `0803020532_18v4 게임 데이터베이스 프로그래밍` 45시간을 Unity 6과 C#으로 재구성한 학생 공개용 운영표입니다. DAY01은 3교시, DAY02~DAY11은 각 4교시, DAY12는 2교시로 운영합니다.

## 운영 원칙

- 관계형 DB는 SQLite와 SQL로, NoSQL의 문서형 DB는 LiteDB와 C# API로 각각 확인합니다. 키-값형 Redis, 컬럼형 Apache Cassandra, 그래프형 Neo4j는 분류와 대표 제품만 확인합니다.
- DAY01에서 만든 `GameShop.db`를 DAY05에서 SQL CRUD로 확장하고, DAY06에서 C# 관리 프로그램으로 다시 구성합니다.
- Unity 6에서는 DB를 직접 운영하는 대신, 읽기 전용 기획 DB와 실행 중 저장 DB의 책임을 구분해 화면 표시를 실습합니다.
- 모든 DAY에는 안내형 실습, 완료 확인, 조건만 제시하는 응용 실습을 둡니다.
- 공개 문서는 학습과 자기 점검용이며, 평가 문항과 채점 기준은 별도 비공개 자료로 관리합니다.

## NCS 반영표

| 능력단위 요소 | 반영 DAY | 학생 확인 산출물 |
| :--- | :--- | :--- |
| 게임 데이터베이스 설계하기 | 01~04 | 저장 위치 판단표, `GameShop.db` 관계형 표·행, 제약 위반 기록, JSON 문서 파일 |
| 게임 데이터베이스 프로그래밍하기 | 05~07, 09~10 | SQLite DB·CRUD 테스트 기록, LiteDB 문서 CRUD 결과 |
| 게임 데이터베이스 응용 프로그래밍하기 | 08, 11~12 | 구매 트랜잭션 결과, Unity 화면 확인, 유지보수 문서 |

## 일자별 운영 흐름

| DAY | 핵심 주제 | 이어지는 결과물 |
| :--- | :--- | :--- |
| 01 | 데이터 선택, SQLite 설치, 첫 `Item` 저장 | `GameShop.db`와 회복 포션 |
| 02~04 | ERD를 관계형 표·행으로 만들기, 제약 위반 실행, JSON 문서 파일 만들기 | 상점 DB·오류 기록·퀘스트 문서 |
| 05~06 | SQL CRUD·조건 조회, C# 스키마·초기 데이터 | `GameShop.db` |
| 07~08 | CRUD, 외래 키, 구매 트랜잭션·롤백 | 테스트 기록과 유지보수 양식 |
| 09~10 | LiteDB 로그·퀘스트 문서 | `GameLogs.db`, `QuestProgress.db` |
| 11~12 | SQLite 기본 표시, LiteDB 로그 선택 확장, 통합 테스트·운영 | 화면 확인과 통합 결과 |

## 공통 실습 결과 확인 규칙

DB 실습은 Console 메시지만 보고 통과로 판단하지 않습니다. SQL 또는 C# 실행 결과와 DB Browser의 실제 행, Unity 실습에서는 화면·Inspector·Console을 함께 확인합니다. 오류가 나면 `증상 → 오류 메시지 → 사용한 DB 파일 경로 → 실행한 SQL 또는 코드 흐름` 순서로 기록합니다.
